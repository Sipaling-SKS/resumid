{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "resumid",
  "scripts": {
    "build": "npm run build --workspace=src/resumid_frontend --if-present",
    "prebuild": "npm run prebuild --workspace=src/resumid_frontend --if-present",
    "pretest": "npm run prebuild --workspace=src/resumid_frontend --if-present",
    "start": "npm start --workspace=src/resumid_frontend --if-present",
    "test": "npm test --workspace=src/resumid_frontend --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/resumid_frontend"
  ]
}
